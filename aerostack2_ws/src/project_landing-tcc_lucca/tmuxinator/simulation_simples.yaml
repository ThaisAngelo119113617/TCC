<%
# Input parameters
drone_namespace        = @settings["drone_namespace"]
simulation_config_file = @settings["simulation_config_file"]
base_launch            = @settings["base_launch"] == "true"
%>

name: <%= drone_namespace %>
attach: false
root: ./
startup_window: mission_monitoring
pre_window: |
  export AS2_EXTRA_DRONE_MODELS=$AS2_EXTRA_DRONE_MODELS:'x500_px4'

windows:
  # Platform
  # Versao simplificada: usa as2_platform_gazebo em vez de PX4 real + as2_platform_pixhawk.
  # O as2_platform_gazebo ja inclui a ponte Gazebo<->ROS2 (drone_bridges) no proprio launch,
  # entao nao precisa chamar drone_bridges.py separado nem subir o PX4-Autopilot/MicroXRCEAgent.
  - platform:
      layout: tiled
      panes:

      - ros2 launch as2_gazebo_assets launch_simulation.py
        use_sim_time:=true
        simulation_config_file:=<%= simulation_config_file %>

      - sleep 4; ros2 launch as2_platform_gazebo platform_gazebo_launch.py
          namespace:=<%= drone_namespace %>
          simulation_config_file:=<%= simulation_config_file %>

  # Basic Robotics Functions
  - basics_robotics_functions:
      layout:
      panes:
        # State Estimation
        - ros2 launch as2_state_estimator state_estimator_launch.py
            namespace:=<%= drone_namespace %>
            config_file:=config/config.yaml
        # Motion Controller
        - ros2 launch as2_motion_controller controller_launch.py
            namespace:=<%= drone_namespace %>
            config_file:=config/config.yaml
            plugin_name:=pid_speed_controller
            plugin_config_file:=config/pid_speed_controller.yaml

  # Basic Behaviors Motion (necessario para arm/offboard/takeoff/land funcionarem
  # via missao_simples.py ou qualquer script Python com DroneInterface)
  - behaviors:
      layout:
      panes:
        - ros2 launch as2_behaviors_motion motion_behaviors_launch.py
            namespace:=<%= drone_namespace %>
            config_file:=config/config.yaml

  # Mission monitoring
  - mission_monitoring:
      layout: tiled
      panes:
        - ros2 run as2_alphanumeric_viewer as2_alphanumeric_viewer_node
            --ros-args -r  __ns:=/<%= drone_namespace %>

  # User: janela livre para rodar missao_simples.py ou qualquer script
  - user:
      layout:
      panes:
        -
